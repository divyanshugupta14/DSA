#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,m;cin>>n>>m;
    int a[n][m];
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<m;j++)
        {cin>>a[i][j];}
    }
    int t[m][n];
    for(int i=0;i<m;i++)
    {
            for(int j=0;j<n;j++)
            {
                t[i][j]=a[j][i];
                cout<<t[i][j]<<" ";
            }
            cout<<"\n";
    }
    return 0;
}
